{
  "name": "cross-game",
  "private": true,
  "version": "2.0.0",
  "description": "CROSS! — Don't Get Hit. Production-grade TypeScript + Three.js arcade game.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "tsc -b",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "quick-liquid": "^0.1.2",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.3",
    "@types/three": "^0.170.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.3",
    "vite-plugin-pwa": "^0.21.1"
  }
}
